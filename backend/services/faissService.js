// backend/services/faissService.js

/**
 * Placeholder FAISS service.
 * Replace this with actual FAISS integration later.
 */

async function searchSimilar(embedding, threshold = 0.95) {
  // TODO: Connect to Python FAISS service
  return null;
}

module.exports = {
  searchSimilar,
};
